import { CustomDropdownTriggerForDirective } from './custom-dropdown-trigger-for.directive';
// test suspended dependency fix needed
describe('CustomDropdownTriggerForDirective', () => {
  it('should create an instance', () => {
    // const directive = new CustomDropdownTriggerForDirective();
    // expect(directive).toBeTruthy();
  });
});
